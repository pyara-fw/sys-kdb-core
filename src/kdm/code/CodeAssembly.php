<?php

namespace  SysKDB\kdm\code;

/**
 * The CodeAssembly represents a logical container for the program elements
 * that were built together (for example, compiled and linked into an executable,
 * so that all variant selection during the compilation and static linking was
 * resolved in a certain coordinated fashion).
 */
class CodeAssembly extends Module
{
}
